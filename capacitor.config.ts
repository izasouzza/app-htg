import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.exemplo.apphtg',
  appName: 'app-htg',
  webDir: 'dist',
  ios: {
    contentInset: 'always',
  },
}

export default config
